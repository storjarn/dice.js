import { randomize } from "./randomize";

describe("randomize", () => {

    describe("randomize()", () => {

        it("exists", () => {
            expect(randomize).toBeTruthy();
        });
    });
});
